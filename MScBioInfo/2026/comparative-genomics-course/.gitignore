# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/

# === Jupyter ===
.ipynb_checkpoints/

# === IDE / OS ===
.vscode/
.idea/
.DS_Store
Thumbs.db
*~
\#*\#

# === Solutions, backups & instructor-only ===
*_solutions.ipynb
*_solved.ipynb
*_bckp.*
*_new*
notebooks/00_preparation.ipynb

# === Generated alignment/tree files (regenerated by running notebooks) ===
# Family-level files (from NB01)
data/slc26.clustalo.*
data/slc26.mafft.*
data/selection2.clustalo.gt01.*

# Subfamily intermediate files (raw MAFFT output, regenerated by 00_preparation)
data/subfamilies/*.aln.fa
# NOTE: *.fa, *.trim.fa, *.trim05.fa, *.nwk are TRACKED — students need them for the assignment

# IQ-TREE intermediate files
*.iqtree
*.bionj
*.mldist
*.ckp.gz
*.model.gz
*.splits.nex
*.contree
*.ufboot
*.treefile
*.log

# NCBI taxonomy database (downloaded by ETE4 on first run)
taxdump.tar.gz

# === Convergence analysis results (regenerated by NB03) ===
data/prestin_convergence_results.csv
data/prestin_entropy.csv

# === Figures (regenerated by notebooks) ===
figures/

# === PyMOL generated ===
data/prestin_convergent.pml

# === Keep these in the repo (source data + pre-computed for students) ===
# !data/selection2_clustalo.fa          # curated family alignment
# !data/selection2.clustalo.seqid2gname.tab  # gene name mapping
# !data/species_classification.tsv      # species + echolocation labels
# !data/cox1.nw                         # COX1 example sequences
# !data/slc23a.mft.gt01                 # SLC23 trimmed alignment
# !data/subfamilies/SLC26A*.fa          # ungapped subfamily sequences
# !data/subfamilies/SLC26A*.trim.fa     # trimmed alignments (for analysis)
# !data/subfamilies/SLC26A*.trim05.fa   # strictly trimmed (for visualization)
# !data/subfamilies/SLC26A*.nwk         # subfamily trees
